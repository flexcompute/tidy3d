# GitHub Actions Workflows

The `public/*` GitHub Actions workflows were removed from this repository.

No GitHub Actions workflow files are currently tracked under this directory. If in-repo automation is added back later, document it here.
