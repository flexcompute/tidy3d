Added `tidy3d troubleshoot` CLI group with `connection`, `environment`, and `report` subcommands that produce paste-friendly support bundles matching the Tidy3D issue-report template, plus `tidy3d.web.diagnose_environment()` and `tidy3d.web.diagnose_report()` helpers for Python users.
