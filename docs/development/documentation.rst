Documentation
==============

Changes list:
- The generated `_autosummary` output only overwrites files when the file does not exist. This means we have full control to configure what attributes and methods we want to display for a given class, and others that are less meaningful.
